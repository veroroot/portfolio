name: Build & Deploy (Pages + PDF)

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ============================
      # 1) Install Pandoc + LaTeX
      # ============================
      - name: Install PDF dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            pandoc \
            texlive \
            texlive-latex-recommended \
            texlive-fonts-recommended \
            texlive-xetex \
            texlive-luatex \
            lmodern \
            fonts-noto \
            fonts-noto-cjk \
            fonts-noto-color-emoji

      # ============================
      # 2) Convert README → PDF (임시 위치)
      # ============================
      - name: Generate PDF (README → portfolio.pdf)
        run: |
          pandoc README.md \
            --from markdown \
            --resource-path=".:./assets" \
            --pdf-engine=lualatex \
            -V mainfont="Noto Sans CJK KR" \
            -V monofont="Noto Sans Mono CJK KR" \
            -V mathfont="Latin Modern Math" \
            -V fallbackfonts="Noto Color Emoji" \
            -o portfolio.pdf

      # ============================
      # 3) Setup Pages
      # ============================
      - name: Setup Pages
        uses: actions/configure-pages@v5     

      # ============================
      # 4) Build GitHub Pages site
      # ============================
      - name: Build GitHub Pages site
        uses: actions/jekyll-build-pages@v1
        with:
          source: .
          destination: ./public

      # ============================
      # 5) Copy PDF to public folder (Jekyll 빌드 후)
      # ============================
      - name: Copy PDF to public folder
        run: |
          chmod -R u+w public || true
          cp portfolio.pdf public/portfolio.pdf

      # ============================
      # 6) Upload artifact for Pages
      # ============================
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: public

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
